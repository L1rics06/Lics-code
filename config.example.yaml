model:
  api_key: "your_api_key_here"
  model_name: "example_model"
  base_url: "https://api.example.com/v1"
  timeout: 30
app:
  workspace: "." #默认工作目录为当前目录
  log_level: "info" #日志级别：info（常规信息）或 debug（详细调试信息，含请求/响应/耗时/用量等）